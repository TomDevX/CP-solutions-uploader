specVersion: "v1"
serviceId: "cp-sols-uploader"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cp-sols-uploader-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
